import React,{useState} from 'react'; import {useAuth} from '../../context/AuthContext.jsx'; import authService from '../../services/authService.js'; import UserAvatar from '../../components/common/UserAvatar.jsx';
export default function ProfilePage(){const {user,setUser}=useAuth();const [form,setForm]=useState({name:user?.name||'',travelStyle:user?.travelStyle||'Adventure',bio:user?.bio||'',preferredCurrency:user?.preferredCurrency||'USD',homeAirport:user?.homeAirport||''});const [message,setMessage]=useState('');async function save(e){e.preventDefault();try{const d=await authService.updateProfile(form);const u=d.user||d;setUser(u);localStorage.setItem('tripmate_user',JSON.stringify(u));setMessage('Profile updated successfully.');}catch(err){setMessage(err.response?.data?.message||'Could not update profile.')}}return <div className="max-w-3xl mx-auto px-4 py-12"><div className="flex items-center gap-4"><UserAvatar user={user} size="lg"/><div><h1 className="text-3xl font-extrabold">Your profile</h1><p className="text-slate-500">{user?.email}</p></div></div><form onSubmit={save} className="mt-8 p-6 rounded-3xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 space-y-4"><Field label="Name" value={form.name} onChange={v=>setForm({...form,name:v})}/><Field label="Travel style" value={form.travelStyle} onChange={v=>setForm({...form,travelStyle:v})}/><Field label="Home airport" value={form.homeAirport} onChange={v=>setForm({...form,homeAirport:v})}/><label><span className="text-sm font-semibold">Bio</span><textarea value={form.bio} onChange={e=>setForm({...form,bio:e.target.value})} className="mt-1 w-full px-4 py-3 rounded-xl border bg-white dark:bg-slate-900" rows="4"/></label>{message&&<p className="text-sm text-violet-600">{message}</p>}<button className="px-5 py-3 rounded-xl bg-violet-600 text-white font-bold">Save profile</button></form></div>}
const Field=({label,value,onChange})=><label className="block"><span className="text-sm font-semibold">{label}</span><input value={value} onChange={e=>onChange(e.target.value)} className="mt-1 w-full px-4 py-3 rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-900"/></label>;
