import React,{useEffect,useState} from 'react'; import {Link} from 'react-router-dom'; import destinationService from '../../services/destinationService.js';
export default function DestinationsPage(){const [items,setItems]=useState([]);const [loading,setLoading]=useState(true);useEffect(()=>{destinationService.list().then(d=>setItems(d.destinations||d.data||[])).catch(()=>{}).finally(()=>setLoading(false))},[]);return <Page><h1 className="text-4xl font-extrabold">Explore destinations</h1><p className="text-slate-500 mt-2">Find inspiration for your next journey.</p>{loading?<p className="mt-8">Loading destinations…</p>:<div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5 mt-8">{items.map(d=><Link key={d._id||d.id} to={`/destinations/${d._id||d.id}`} className="overflow-hidden rounded-2xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800"><img src={d.image||d.coverImage} className="w-full h-48 object-cover"/><div className="p-4"><h2 className="font-bold">{d.name}</h2><p className="text-sm text-slate-500 mt-1">{d.country}</p></div></Link>)}</div>}</Page>}; const Page=({children})=><div className="max-w-6xl mx-auto px-4 py-12">{children}</div>;
