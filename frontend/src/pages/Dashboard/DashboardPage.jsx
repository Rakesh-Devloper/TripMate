import React,{useEffect} from 'react'; import {Link} from 'react-router-dom'; import {useAuth} from '../../context/AuthContext.jsx'; import {useTrips} from '../../context/TripContext.jsx';
export default function DashboardPage(){const {user}=useAuth();const {trips,loadTrips,loading}=useTrips();useEffect(()=>{loadTrips().catch(()=>{})},[]);return <Page><h1 className="text-4xl font-extrabold">Welcome, {user?.name?.split(' ')[0]||'Traveler'} 👋</h1><p className="text-slate-500 mt-2">Your travel command center.</p><div className="grid sm:grid-cols-3 gap-4 mt-8"><Stat label="Saved trips" value={trips.length}/><Stat label="Travel style" value={user?.travelStyle||'Not set'}/><Stat label="Status" value="Ready to explore"/></div><div className="mt-8 flex gap-3"><Link className="px-5 py-3 rounded-xl bg-violet-600 text-white font-bold" to="/ai-planner">Plan a new trip</Link><Link className="px-5 py-3 rounded-xl border border-slate-200 dark:border-slate-700 font-bold" to="/trips">View trips</Link></div><section className="mt-10"><h2 className="text-2xl font-bold">Recent trips</h2>{loading?<p className="mt-4 text-slate-500">Loading…</p>:trips.length?<div className="mt-4 grid md:grid-cols-2 gap-4">{trips.slice(0,4).map(t=><Link key={t._id||t.id} to={`/trip/${t._id||t.id}`} className="p-5 rounded-2xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800"><b>{t.title||t.destination}</b><p className="text-sm text-slate-500 mt-1">{t.destination}</p></Link>)}</div>:<p className="mt-4 text-slate-500">No saved trips yet.</p>}</section></Page>}
const Stat=({label,value})=><div className="p-5 rounded-2xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800"><div className="text-sm text-slate-500">{label}</div><div className="text-xl font-extrabold mt-1">{value}</div></div>; const Page=({children})=><div className="max-w-6xl mx-auto px-4 py-12">{children}</div>;
