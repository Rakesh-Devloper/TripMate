import React from 'react';
import { Link, NavLink, useNavigate } from 'react-router-dom';
import { Plane, LogIn, LogOut, UserCircle } from 'lucide-react';
import ThemeToggle from '../common/ThemeToggle.jsx';
import UserAvatar from '../common/UserAvatar.jsx';
import { useAuth } from '../../context/AuthContext.jsx';
const links=[['/','Home'],['/ai-planner','AI Planner'],['/destinations','Destinations'],['/trips','Trips'],['/community','Community'],['/pricing','Pricing']];
export default function Navbar(){const {user,isAuthenticated,logout}=useAuth();const nav=useNavigate();return <header className="sticky top-0 z-50 border-b border-slate-200/70 dark:border-slate-800 bg-white/90 dark:bg-[#08101d]/90 backdrop-blur-xl"><div className="max-w-7xl mx-auto px-4 py-3 flex items-center gap-4"><Link to="/" className="flex items-center gap-2 font-extrabold text-xl text-slate-900 dark:text-white mr-auto"><span className="w-10 h-10 rounded-xl bg-gradient-to-tr from-violet-600 to-blue-600 text-white grid place-items-center"><Plane/></span>TripMate</Link><nav className="hidden lg:flex items-center gap-1">{links.map(([to,label])=><NavLink key={to} to={to} className={({isActive})=>`px-3 py-2 rounded-xl text-sm font-semibold ${isActive?'bg-violet-100 text-violet-700 dark:bg-violet-950/60 dark:text-violet-300':'text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-slate-800'}`}>{label}</NavLink>)}</nav><ThemeToggle/>{isAuthenticated?<div className="flex items-center gap-2"><button onClick={()=>nav('/profile')} title="Profile"><UserAvatar user={user} size="sm"/></button><button onClick={logout} className="p-2 rounded-lg text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-slate-800" title="Logout"><LogOut size={18}/></button></div>:<Link to="/login" className="inline-flex items-center gap-2 px-4 py-2 rounded-xl bg-slate-900 text-white dark:bg-white dark:text-slate-900 text-sm font-bold"><LogIn size={16}/> Login</Link>}</div></header>}
